# MSM-VaR Model Dependencies
# ==========================
# Install with: pip install -r requirements.txt

# Core Scientific Computing
numpy>=1.20.0
pandas>=1.3.0

# Statistical Functions & Optimization
scipy>=1.7.0

# Data Visualization
matplotlib>=3.4.0

# Financial Data Download
yfinance>=0.2.0

# GARCH / EGARCH / GJR-GARCH Models
arch>=5.3.0

# Solana DeFi Data
httpx>=0.24.0

# Axiom Trade DEX Aggregator (read-only data)
axiomtradeapi>=1.1.0

# Jupiter Swap API (trade execution) — local signing deps
solders>=0.20.0
base58>=2.1.0

# API Server
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0
websockets>=11.0

# Metrics
prometheus-fastapi-instrumentator>=6.0

# Async Caching (Redis + in-memory fallback)
cashews[redis]>=7.0

# Hawkes Performance (optional — graceful fallback if unavailable)
numba>=0.59.0
tick>=0.7; python_version<"3.12"

# EVT (pyextremes) & Rough Volatility (fbm) — optional engines
pyextremes>=2.3.0
fbm>=0.3.0

# Structured Logging
structlog>=24.0

# Testing
pytest>=7.0
