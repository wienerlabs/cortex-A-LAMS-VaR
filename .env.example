# ============= SOLANA CONFIGURATION =============
SOLANA_RPC_URL=
SOLANA_RPC_URL_SECONDARY=
SOLANA_RPC_COOLDOWN_MS=60000
SOLANA_RPC_MAX_FAILURES=3
SOLANA_WS_URL=
SOLANA_WS_URL_SECONDARY=
SOLANA_WS_MAX_FAILURES=3
SOLANA_PRIVATE_KEY=
SOLANA_COMMITMENT=confirmed
HELIUS_API_KEY=
HELIUS_RPC_URL=
JITO_RPC_URL=
SIMULATION_MODE=false
# RPC health monitoring HTTP port
HEALTH_PORT=9090

# ============= AXIOM TRADE DEX AGGREGATOR =============
# No API key — uses email/password + OTP login, then stores tokens
# First run: python -c "from cortex.data.axiom import login_interactive; login_interactive()"
AXIOM_EMAIL=
AXIOM_PASSWORD=
AXIOM_AUTH_TOKEN=
AXIOM_REFRESH_TOKEN=

# ============= JUPITER SWAP API (TRADE EXECUTION) =============
JUPITER_API_KEY=
JUPITER_RPC_URL=
JUPITER_SLIPPAGE_BPS=100
JUPITER_RPM_LIMIT=600

# ============= SOLANA DATA PROVIDERS =============
BIRDEYE_API_KEY=
BIRDEYE_RPM_LIMIT=60
SOLSCAN_API_KEY=

# ============= CEX API KEYS =============
BINANCE_API_KEY=
BINANCE_SECRET_KEY=
KRAKEN_API_KEY=
COINBASE_API_KEY=
# Timeout for CEX deposit confirmation (ms)
CEX_DEPOSIT_TIMEOUT_MS=600000

# ============= CRYPTO NEWS SOURCES =============
NEWSDATA_API_KEY=
CC_API_KEY=
CRYPTOPANIC_TOKEN=
CRYPTOPANIC_API_KEY=

# ============= SENTIMENT ANALYSIS =============
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHANNELS=

# ============= TWITTER API CREDENTIALS =============
TWITTER_BEARER_TOKEN=
TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_CLIENT_ID=

# ============= PUMP.FUN API (AGGRESSIVE MODE ONLY) =============
PUMPFUN_API_KEY=
PUMPFUN_WALLET_PUBLIC=
PUMPFUN_WALLET_PRIVATE=

# ============= TRADING MODE =============
# NORMAL: Conservative trading (min health 60, no memecoins, no Pump.fun)
# AGGRESSIVE: Higher risk (min health 40, allows memecoins, allows Pump.fun)
TRADING_MODE=NORMAL

# ============= CACHE (CASHEWS) =============
# Redis URL for caching (leave empty for in-memory fallback)
REDIS_URL=redis://localhost:6379/0
CACHE_ENABLED=true
CACHE_DEFAULT_TTL=300

# ============= MODEL PERSISTENCE (REDIS) =============
# Uses REDIS_URL by default; set separately if you want a different DB
PERSISTENCE_ENABLED=true
PERSISTENCE_REDIS_URL=redis://localhost:6379/1
PERSISTENCE_KEY_PREFIX=cortex:store:

# ============= WAVE 11B: MODEL ENHANCEMENT ENGINES =============
# Copula engine: "native" (scipy-based) or "vine" (pyvinecopulib)
COPULA_ENGINE=native
# Portfolio optimization: "native" (numpy) or "skfolio"
PORTFOLIO_OPT_ENGINE=native
# Default exchange for ccxt data feed
CCXT_DEFAULT_EXCHANGE=binance

# ============= OPTIONAL =============
HF_TOKEN=
HF_REPO_ID=cortex-ai/models

# ============= API SERVER =============
API_HOST=0.0.0.0
API_PORT=8000

# ============= API SECURITY =============
# Leave CORTEX_API_KEYS empty to disable auth in dev
CORTEX_API_KEYS=
CORTEX_ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000
CORTEX_RATE_LIMIT_READ=60
CORTEX_RATE_LIMIT_WRITE=10
